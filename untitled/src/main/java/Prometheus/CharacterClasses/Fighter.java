package Prometheus.CharacterClasses;

import Prometheus.Characters.Player;

public class Fighter extends Player {
    public Fighter(String name, String ancestry, int age, int[] stats) {
        super(name, ancestry, age, stats);
    }
}
